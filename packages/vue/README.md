# @water-ui/vue

Vue renderer package for `VerifiedSchemaUI`.

This package provides:

- `WaterRenderer`
- `WaterStreamRenderer`
- `WaterRuntimeProvider`
- `NodeRenderer`
- `SlotRenderer`
- recursive node and slot rendering
- runtime data and action binding
- render diagnostics
- safe fallback rendering

`@water-ui/vue` renders verified UI only. Pass raw model output through
`@water-ui/core` verification before rendering.

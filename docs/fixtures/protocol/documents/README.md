# Document Protocol Fixtures

Gate 2 and Gate 3 document fixtures prove:

- valid `water.ui.document` inputs parse
- invalid versions are rejected
- missing roots are rejected
- missing node references are rejected
- unknown component types are rejected during verification
- invalid props are diagnosed
